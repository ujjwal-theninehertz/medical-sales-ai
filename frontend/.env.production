# Empty on purpose: the frontend is served from the SAME origin as the API (Caddy serves
# dist/ and reverse-proxies /api/* to the backend container), so all requests are relative
# and nothing breaks if this server IP or hostname ever changes. Do not set an absolute URL.
VITE_API_BASE_URL=
